---
name: fusion-skill-testing
description: "用户想测试/审查/验证/体检一个 skill（刚创作完、改完、或老 skill 想体检）时使用。全平台通用，不绑定任何特定 agent 平台。覆盖：全路径行为测试、静态质量审查（交叉引用/对称性/UX/过度设计/写作质量/token 效率/安全）、缺陷分级（P0/P1/P2）与修复方案、token 效率四级评级、可选五维质量总结、回归测试。触发词：测试这个skill、审查skill、帮我测一下、检查一下、评估一下、诊断一下、帮我看看这个skill行不行、体检一下、skill 有没有问题、回归测试；English: test this skill / review skill / audit skill / check this skill / evaluate this skill / diagnose this skill / skill quality check / regression test。"
version: "1.6.7"
category: "开发工具"
author: "Bangbang fusion"
agent_created: true
---

# fusion-skill-testing · Skill 质量测试

> © 2026 Bangbang fusion，CC BY-NC-SA 4.0 授权

## 目标

**核心用途**：把任何一个 skill（单文件或多文件包）测穿——静态审查 + 全路径行为测试 + 有效性核对 + 分级修复方案 + 回归，产出带证据的缺陷清单和「可用 / 待修复」结论。

**适用对象**：三类人——写、改、维护 skill 的人；上架/分享前想自检的人；团队想立统一质检标准的人。定位**全平台通用**（不限任何特定 agent 平台），从用户视角把 skill 打磨到「好用且真正能用」，不迎合单一平台的审核规则。

**预期输出**：一份报告，六节——①路径矩阵 ②逐路径结果 ③有效性核对 ④缺陷清单（每条带文件+行号+修复方案）⑤token 效率评级 ⑥总结；深度模式追加可选「五维质量总结」与「实用性独立轴」。模板见 [report.md](reference/report.md)。报告面向两类读者：决策者只看 ⑥ 总结与结论即可（无需懂代码），修复者看 ④ 缺陷清单的文件+行号+修复方案。

**怎么读这份规范（阅读路径）**：新手只读 [README.md](README.md) 并跑「快速（gate）」模式即可上手；想看懂结论与缺陷分级，看 [report.md](reference/report.md)；要立自己的测试标准或深挖判据，再按需翻对应 reference（[defects.md](reference/defects.md) 缺陷库 / [matrix.md](reference/matrix.md) 测试矩阵 / [evaluation-principles.md](reference/evaluation-principles.md) 九原则 / [token-efficiency.md](reference/token-efficiency.md) 评级细则 / [quality-perspectives.md](reference/quality-perspectives.md) 五维判据）。无需一次读完。

## 前置条件与约束

- **前置**：目标必须是含 `SKILL.md` 的 skill 包目录；宿主需能读文件、跑 Python（形式层检查脚本）；纯离线可用。
- **约束**：本工具**只诊断、不改被测 skill**——修复方案交给用户、改不改由用户定；全程只读，不修改被测文件；测试者不替 spec 做主（路径由被测 skill 文档决定，不是由「更合理」的流程决定）。

## 测试对象与参数边界

- **测试对象**：任意位置的 skill 包目录；用户不指明时默认「当前对话涉及 / 最近修改」的 skill。
- **模式参数（三档，默认「标准」）**：
  - **快速（gate）**：形式层 + 安全审查 + 交叉引用 + 描述真实性——秒级，创作早期/每次迭代后跑（像 lint）
  - **标准（默认）**：gate + 行为路径 + 失败路径 + token 评级——常规完整评测
  - **深度**：标准 + 基线对照 + 组合级 + 可选五维质量总结与实用性独立轴（判据见 [quality-perspectives.md](reference/quality-perspectives.md)）——完整评测 / 深度优化
  - 三档深度递增、非互斥；快速/标准可随时升级到更深一档。
  - **一句话直觉（说给用户听）**：快速=量体温、标准=常规体检、深度=全项+影像。**触发词绑定**：用户说「快速测/秒查」→ 快速档；「深度测/全面审计」→ 深度档；未指明或说「测试这个skill/审查」→ 标准档。
- **拒绝场景（该拒则拒，不硬接）**：非 skill 质量相关请求（写代码/翻译/闲聊）；目标不是 skill 包（普通文档/网站/数据表）；纯主观创意评价（无客观判据）。宿主无法读文件/跑脚本时降级为静态审查（凭模型知识 + 用户贴出的内容），并明说行为层测不了。
- **规模边界**：单包文件数 ≤ 50 且 SKILL.md ≤ 400 行 → 标准流程全测；超限包两种走法（报告「测试范围」须明示走法与原因）——① **快速走法**：门禁层 + 主流程全链路，完整行为层按用户指定子集测（避免长流程中途失真、报告失控）；② **分批走法**：按子目录/模块把包切成 2-3 批，每批单独出一份子报告，最后合并结论。
- **深度模式适用边界**：**适用**——正式交付/发布前的审计级验证、需要基线对照与五维总结的深度优化；**不适用**——日常快速打磨（标准档足够）、超大包（先门禁 + 主流程）、无 Python 宿主（降级静态审查）。不确定时默认标准档，测试中途可升级。

## 评价维度与权重

五个并列维度，统一由缺陷分级（P0/P1/P2）承载；token 效率另设四级评级。**安全为独立红线**（步骤 1.8 + formcheck ⑦ + defects.md「安全缺陷」节），不计入五维优化维度，触碰即「终止交付建议」。

- **门禁层**（存在 P0 即「待修复」，不可交付使用）：×正确性（规则与功能一致、流程线状态无缺失）、×引用（§编号/链接/锚点与实指一致）
- **优化层**（门禁通过后评估）：×token 效率（路径是否最优）、×过度设计（机制/参数/文档冗余；操作化判据：可算法化替代且更经济+更精确却未替代 = 残留，见步骤 1.7）、×UX（复杂度/反馈质量/入口可见性）
- **优先级**：正确性 > 引用 > token 效率 > 过度设计 > UX——**先对后省**：功能错误的 skill 再省 token 也无意义；token 效率评级只对功能正确的 skill 生效，存在 P0 时标 N/A。
- **权重随场景流动**（原则 7）：上述优先级是通用默认值。To C 重意图精准与体验；To B/金融重容错与可观测；实时场景重执行效率。调整时须声明三件事（明细见 [evaluation-principles.md](reference/evaluation-principles.md)）。
- **标准版本**（原则 8）：本标准 **v1.6**（2026-08-25 按官方测评重构：目标/前置/参数边界显式化，7 步改为「输入→处理→输出」，补输出示例）；**v1.6.1**（2026-08-25 自测修正：formcheck 检查项描述与实现对齐——⑤行数需 `--limit`、⑥禁用词为可选启用；安全为独立红线不计入五维）；**v1.6.2**（2026-08-25 清除禁用词外部文件依赖：⑥ 禁用词改为 `--banned` 参数传入；按二轮测评补强——中断恢复、规模边界、触发词、FAQ 与完整走查案例）；**v1.6.3**（2026-08-25 移除 ⑥ 禁用词检查：低频能力可被 grep 等价替代、未满足算法化双条件收益，机制删除、能力保留为按需 grep 人工核验）；**v1.6.4**（2026-08-27 按三轮测评改进：formcheck 报错提示双轨制（人话+技术）、中断恢复三步化、README 真实对话样例、降级预期管理前移）；**v1.6.5**（2026-08-27 按四轮测评改进：SKILL.md 阅读路径引导、无法执行三段式统一报错、作者避坑自查清单、触发词补诊断类、深度模式适用边界、降级声明正面化）；**v1.6.7**（2026-09-01 按六轮测评响应：README 新增「第一次使用·手把手走查」完整案例、避坑清单与反模式速认标注进阶章节（新手可跳过）、简介声明纯本地零联网、深度档实用性轴补「对你有啥用」转译——压感知复杂度、回应"缺手把手完整案例"扣分）；**v1.6.6**（2026-09-01 按五轮测评 + 实用性轴改进：formcheck 跳过项显式化（防止"读不到"误报"没内容"）、无 Python 覆盖差异透明化、三档模式体检直觉 + 触发词绑定档位、常见反模式速认表、超大包分批法（快速走法/分批走法）、非技术读者声明、实用性镜头与基线量化（仅发布/上架场景必测）、缺陷库新增价值主张缺失/无代价节省证据/缺复用场景、报告新增实用性独立轴节与四象限结论）。耐久条款优先维护；易朽条款到期复审或退役。
- 九原则完整映射见 [evaluation-principles.md](reference/evaluation-principles.md)；token 评级细则见 [token-efficiency.md](reference/token-efficiency.md)。

## 流程七步（每步 = 输入 → 处理 → 输出）

### 步骤 1 · 静态审查
- **输入**：目标 skill 包全部文件（SKILL.md + reference + 工具脚本）
- **处理**：
  1. 通读建立事实基线（每个文件几步、入口顺序、交付物形态、术语口径）
  2. 跑 `python tools/formcheck.py <skill_dir>`（①frontmatter ②链接 ③锚点 ④孤儿 ⑦安全 **默认启用**；⑤行数红线需 `--limit` 配置；脚本自带 `--help` 与每条 FAIL 的修复提示）；目标 skill 有自己的 formcheck 则优先用之。品牌词/禁用词零残留等按需附加检查用 grep 人工核验（如 `grep -ri "词" .`），不内置脚本
  3. 交叉引用核对：所有「见 X §N」/链接/锚点/术语与实指一致，错位 = P0
  4. 对称性核对：声称覆盖的每个分支是否有对等设计，不对称 = P0
  5. 质量镜头（判据见 [defects.md](reference/defects.md#质量层缺陷)）：premature completion / duplication / no-op / negation / sediment / sprawl / 完成标准不可检查 / 双写同源 / 参数幻觉 / 指针措辞；**实用性镜头（仅发布/上架/收录场景必测——有受众才评；个人一次性工具跳过并标注「未评估实用性」）**：①价值主张核对——文档是否含「给谁 + 何时 + 解决什么痛点」场景句与收益量化句，缺引用按缺陷记（`价值主张缺失`）；②方法可执行性核对——给的是动作链还是「要重视 X」式忠告，纯忠告按缺陷记（`无代价节省证据` / `缺复用场景` 判据见 [defects.md](reference/defects.md#质量层缺陷)）
  6. token 效率初判（判据见 [token-efficiency.md](reference/token-efficiency.md)）：全量重读/重复计算/可合并未合并/无必要输出，标记嫌疑点待行为层量化
  7. 算法化替代评估：双条件（更经济 ∧ 更精确）**同时满足才替代并删原文**，任一不满足保留原文
  8. 安全审查五查（红线清单见 [defects.md](reference/defects.md#安全缺陷)）：①敏感信息 ②危险操作 ③权限请求 ④网络行为 ⑤依赖；红线触碰任一 → **终止交付建议**（诊断继续、数据照收）
- **输出**：静态问题按 P0/P1/P2 列出（每条带文件+行号）；token 嫌疑点；算法化与安全判定各有证据

### 步骤 2 · 设计测试矩阵
- **输入**：步骤 1 的事实基线 + 被测 skill 的入口/分支/导航结构
- **处理**：
  1. 路径矩阵：枚举入口/分支/边界/导航路径，每条含「触发对象（具体示例）+ 路径 + 验证重点」（模板见 [matrix.md](reference/matrix.md#路径矩阵)）
  2. 笛卡尔积抽查：输入类型 × 模式 × 状态，至少覆盖首条带阶段词/跳步/提前产出/回退/重做/跳关/退出/中断恢复/模糊表达/边界输入（见 [matrix.md](reference/matrix.md#笛卡尔积矩阵)）
  3. 每个分支至少 1 条、主流程全链路 1 条、每类导航动词 1 条
  4. 触发/拒识路径（原则 2）：能力边界**内**样本（该调）与**外**样本（该拒）各测
  5. 组合级（原则 5）：与相邻 skill 联动，查权限越权/调用成环/成本复利
- **输出**：完整路径矩阵 + 必测组合 + 触发/拒识/组合级路径，每条有触发对象与验证重点

### 步骤 3 · 逐路径执行
- **输入**：步骤 2 的矩阵
- **处理**：
  1. 真实对话逐条跑，**忠实于 spec**（只按被测文档写明的规则推演），记录实际输出
  2. 捕获典型失败：跳步/占位符泄漏/菜单漂移/复读
  3. AI 自造 spec 没有的结构接住用户 → 标记「临场 reinvent」，该路径不计通过，按 P0/P1 记缺陷
  4. 输出不可信场景（时效/外部知识）主动联网核验，看有无溯源要求
  5. 记录 token 观察指标：工具调用次数/重复读取文件数/中间步骤数/可省输出
  6. 失败路径（原则 4）：异常输入/资源缺失/重复操作，验证优雅降级/幂等/早失败/兜底
  7. 基线对照（原则 6）：同任务裸 prompt 跑一遍，对比「skill 加持」是否更好；不如零成本方案要如实记录。**发布/上架场景量化**：同时记录裸 prompt 路径与 skill 加持路径的步骤数/轮数，产出「省 N 步 / 省 M 轮」的可核对数字（价值主张缺量化证据按 `无代价节省证据` 记）
- **输出**：每条路径「PASS / FAIL / reinvent」初判 + 实际输出 + token 指标，无凭印象填结果

### 步骤 4 · 有效性核对
- **输入**：步骤 3 的逐路径结果
- **处理**：逐条复核「忠于 spec」还是「偏离 spec」（对照被测源码核对落位）；对「看似通过」做反向验证是否真走到声称的机制
- **输出**：每路径标注忠于/偏离 + 源码对照；无效路径就地更正结论并记缺陷

### 步骤 5 · 分级与修复方案
- **输入**：全部缺陷候选
- **处理**：
  1. 分级 🔴P0 / 🟠P1 / 🟡P2（定义与示例见 [defects.md](reference/defects.md)，权威不在此复述）
  2. 每条记：编号 / 现象 / 证据（文件+行号）/ 机制链 / 影响 / **改哪个文件 + 怎么改**
  3. 新缺陷类型追加到 [defects.md](reference/defects.md)（跨 skill 缺陷记忆）
  4. token 效率四级评级（🟢/🟡/🟠/🔴/N/A，判据见 [token-efficiency.md](reference/token-efficiency.md)）
- **输出**：缺陷清单（每条有证据与修复落点，同类合并去重）+ token 评级（带量化依据）

### 步骤 6 · 回归测试
- **输入**：用户修复后的 skill + 原缺陷清单
- **处理**：重跑被修复路径 + 相邻路径 + 全部主流程路径；确认无新回归（修 A 引入 B 是常态）；仍有 FAIL 不宣告收敛
- **输出**：回归结论（P0 全通过 / 仍有失败并定位）

### 步骤 7 · 产出报告
- **输入**：步骤 1-6 全部结果
- **处理**：按 [report.md](reference/report.md) 六节输出（①路径矩阵 ②逐路径 ③有效性 ④缺陷清单 ⑤token ⑥总结；深度追加五维质量总结与实用性独立轴，后者发布/上架场景必测）；区分已修复/待用户决策/新增缺陷记忆；附关键证据行（文件+行号）
- **输出**：报告六节齐全，缺陷可追溯到证据，结论明确「可用 / 待修复」；报告格式与示例见 [report.md](reference/report.md)

---

## 纪律

- **测试者不替 spec 做主**：路径怎么走由被测 skill 文档决定，不是由「更合理」的流程决定。
- **中断恢复（三步）**：测试中遇工具失败/网络中断/宿主卡顿——①**检测**：确认中断点与已完成的步骤；②**落盘**：已产出结论立即写入报告并标注「未完成（中断于步骤 N）」；③**续跑**：从中断步骤继续，已通过路径不重跑。中断即未完成，如实标注、不宣告收敛。
- **无法执行三段式**：模型引导过程中遇宿主权限不足/文件损坏/资源缺失等执行层错误，统一按「①无法执行 + ②原因一句话 + ③替代方案」回应（如：无法读取该文件——权限不足——请授权后重试，或改用静态审查模式），不裸报错、不假装完成。
- **有效性问题优先于数量**：10 条忠实路径胜过 20 条 reinvent 的「通过」。
- **缺陷必须带证据**：现象 + 文件行号；缺证据的怀疑写进报告但不算缺陷。
- **分数是体温计不是靶子**（原则 9）：评级只用于诊断定位，不贴终身标签，不为「评级好看」优化目标 skill。
- **报告与缺陷库是资产**：测完把新缺陷类型沉淀回 [defects.md](reference/defects.md)，矩阵沉淀回 [matrix.md](reference/matrix.md)。
